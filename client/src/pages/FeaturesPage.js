import { Container } from "reactstrap";

const FeaturesPage = () => {
    return (
        <Container fluid>
            <p>This will be the Features Page</p>
        </Container>
    );
};

export default FeaturesPage;
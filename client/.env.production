# This file is committed to git (CRA needs it present at build time). Never put a secret
# here - only public, build-time values like this one. See CLAUDE.md's Commands section.
REACT_APP_API_BASE_URL=https://music-historeum.vercel.app/
